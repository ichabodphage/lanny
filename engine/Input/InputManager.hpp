#include <SFML/Graphics.hpp>
#include <map>

namespace lny{
	/*
	*	class that manages inputs from the scene, each input is stored 
	*	within an orded map with the sf event as a key and a function 
	*	as the parameter.
	*/
	class InputHandler{

	};
}